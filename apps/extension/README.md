# Chrome Extension

Planned Manifest V3 adapter for YouTube and explicitly approved HTML5-video
sites. Initial required access is YouTube-only. Generic sites must use
`optional_host_permissions` and an explicit per-site user grant. `<all_urls>`,
remote executable code, and silent caption uploads are forbidden.
