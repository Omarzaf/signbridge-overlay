{
  "name": "signbridge-overlay",
  "version": "0.0.0",
  "private": true,
  "description": "A lightweight, human-reviewed sign-language overlay protocol and runtime for online and offline video.",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "node tools/verify-baseline.mjs",
    "lint": "node --check tools/verify-baseline.mjs && node --check tests/baseline/verify-baseline.test.mjs && node tools/verify-baseline.mjs",
    "test": "node --test tests/baseline/verify-baseline.test.mjs",
    "verify": "node tools/verify-baseline.mjs && node --test tests/baseline/verify-baseline.test.mjs"
  }
}
